@model IEnumerable<VetClinicWeb.Models.FacilityViewModel>

@{
    ViewData["Title"] = "Facility";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h4>List of facilities</h4>

<form asp-controller="Facility" asp-action="Index" method="get">
    <div class="container-md mt-3 mb-3" style="width:50%">
        <div class="row">
            <div class="col">
                @Html.DropDownList("Option", (IEnumerable<SelectListItem>) ViewBag.Options, "Choose", new {@class="form-control"})
            </div>
            <div class="col-8">
                <div class="input-group-append">
                    <input type="text" name="search" placeholder="Search" class="form-control"/>
                    <input type="submit" value="Search" class="btn btn-primary"/>
                </div>
            </div>
        </div>
    </div>
</form>

<p>
    <a asp-action="Create">Create New</a>
</p>

<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Address)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PhoneNumber)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Address)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PhoneNumber)
            </td>
            <td>
                @Html.ActionLink("Update", "Update", new { id=item.FacilityId }) |
                @Html.ActionLink("Delete", "ShowDelete", new { id=item.FacilityId })
            </td>
        </tr>
}
    </tbody>
</table>
